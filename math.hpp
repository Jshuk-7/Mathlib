#ifndef MATHLIB_MATH
#define MATHLIB_MATH

namespace mathlib::math
{
    int absolute(int x);

    float absolute(float x);

    double absolute(double x);

    int power(int base, int power);
}

#endif